-r requirements.txt
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
python-multipart>=0.0.20,<1
httpx>=0.27,<1
Authlib>=1.7.2,<2
psycopg[binary]>=3.3.4,<4
cryptography>=46,<49
# Mã hoá QR cho ghép cặp điện thoại. Thuần Python, không kéo theo phụ thuộc nào; tự viết thì
# phải làm Reed-Solomon và mặt nạ, vài trăm dòng chứ không phải vài dòng.
segno>=1.6,<2
pystray==0.19.5; sys_platform == "win32"
